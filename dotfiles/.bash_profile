source $HOME/.commonrc

HISTSIZE=1000
HISTFILESIZE=2000

# don't put duplicate lines or lines starting with
# a space in the history.
HISTCONTROL=ignoreboth

# append to the history file instead of overwriting it
shopt -s histappend

